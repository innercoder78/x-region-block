{
  "name": "x-region-block",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "clean": "node -e \"import('node:fs/promises').then(({rm}) => rm('dist', { recursive: true, force: true }))\"",
    "build:chrome": "rollup --config --environment BROWSER:chrome",
    "build:firefox": "rollup --config --environment BROWSER:firefox",
    "build": "npm run clean && npm run build:chrome && npm run build:firefox",
    "validate:build": "node scripts/validate-build.js",
    "lint": "eslint .",
    "test": "vitest run",
    "check": "npm run lint && npm test && npm run build && npm run validate:build"
  },
  "devDependencies": {
    "@eslint/js": "9.32.0",
    "eslint": "9.32.0",
    "globals": "16.3.0",
    "rollup": "4.45.1",
    "vitest": "3.2.4"
  }
}
